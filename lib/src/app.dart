import 'package:flutter/material.dart';

class App extends StatelessWidget {
  Widget build(context){
    return MaterialApp(
      title: 'Log me in!',
      home: Scaffold(
        body: Text('Here will be the form!'),
      ),
    );
  }
}